import { makeStyles } from "@mui/styles";

const useStyles = makeStyles(() => ({
  container: {
    margin: "40px 0",
  },

  loginFieldContainer: {
    margin: "50px 0",
  },

  backButton: {
    textDecoration: "none",
    color: "black"
  }
  
}));


export default useStyles;
